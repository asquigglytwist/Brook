<Window x:Class="Brook.MainWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Brook.MainWin"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Menu x:Name="mnuTopBar" DockPanel.Dock="Top" MinHeight="24" />
        <ListView x:Name="lsvSoundFiles" VerticalAlignment="Top" DockPanel.Dock="Bottom">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Album}"
                      Header="Album" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Title}"
                      Header="Title" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Year}"
                      Header="Year" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Track}"
                      Header="Track" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Duration}"
                      Header="Duration" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Lyrics}"
                      Header="Lyrics" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=Comment}"
                      Header="Comment" />
                    <GridViewColumn DisplayMemberBinding=
                          "{Binding Path=SourceAudioFile}"
                      Header="SourceAudioFile" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
